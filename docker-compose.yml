version: '3.8'
services:
  web:
    build: .
    ports:
      - "8081:80"
    environment:
      - APP_TITLE=MyDockerProject
    networks:
      - app-network

  redis:
    image: redis:alpine
    networks:
      - app-network

networks:
  app-network:
